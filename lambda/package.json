{
  "name": "first-claudia",
  "version": "1.0.0",
  "description": "A hello world test",
  "main": "hw.js",
  "scripts": {
    "create-app": "claudia create --region us-east-1 --api-module hw --profile coopstools --name claudia-api --keep",
    "update-app": "claudia update --profile coopstools --keep",
    "test": "curl $npm_claudia_lambda_role"
  },
  "author": "James@Coopstools.com",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.122.0",
    "claudia-api-builder": "^2.5.1"
  },
  "devDependencies": {
    "claudia": "^2.14.2"
  }
}
